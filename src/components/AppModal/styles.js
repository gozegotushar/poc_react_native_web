import { StyleSheet } from 'react-native';

export default (theme) =>
  StyleSheet.create({
    container: {
      backgroundColor: 'black',
      opacity: 0.8,
      position: 'absolute',
      justifyContent: 'center',
      alignItems: 'center'
    },
    textStyle: {}
  });
